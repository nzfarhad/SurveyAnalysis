% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/single_select.R
\name{single_select}
\alias{single_select}
\title{Single Select Question Analysis}
\usage{
single_select(df, ques, disag, level, show_view = FALSE, format_output = TRUE)
}
\arguments{
\item{df}{A data frame containing the survey data}

\item{ques}{Character string specifying the column name of the question to analyze}

\item{disag}{Character string specifying the disaggregation variable name}

\item{level}{Character string specifying the disaggregation level}
}
\value{
A data frame with analysis results including percentages, counts, and metadata
}
\description{
Analyzes single-select questions from survey data, calculating percentages and counts.
This function can be used standalone or as part of the main analysis workflow.
}
\examples{
# Create sample data
survey_data <- data.frame(
  gender = c("Male", "Female", "Male", "Female", "Male"),
  region = c("North", "South", "North", "South", "East")
)

# Analyze gender overall
gender_results <- single_select(df = survey_data, 
                               ques = "gender", 
                               disag = "all", 
                               level = "all")
print(gender_results)

# Analyze gender by region
gender_by_region <- single_select(df = survey_data, 
                                 ques = "gender", 
                                 disag = "region", 
                                 level = "North")
print(gender_by_region)

}
