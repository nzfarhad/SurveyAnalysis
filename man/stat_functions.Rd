% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stat_functions.R
\name{stat_mean}
\alias{stat_mean}
\alias{stat_median}
\alias{stat_sum}
\alias{stat_1stq}
\alias{stat_3rdq}
\alias{stat_min}
\alias{stat_max}
\title{Statistical Analysis Functions}
\usage{
stat_mean(df, ques, disag, level, show_view = FALSE, format_output = TRUE)

stat_median(df, ques, disag, level, show_view = FALSE, format_output = TRUE)

stat_sum(df, ques, disag, level, show_view = FALSE, format_output = TRUE)

stat_1stq(df, ques, disag, level, show_view = FALSE, format_output = TRUE)

stat_3rdq(df, ques, disag, level, show_view = FALSE, format_output = TRUE)

stat_min(df, ques, disag, level, show_view = FALSE, format_output = TRUE)

stat_max(df, ques, disag, level, show_view = FALSE, format_output = TRUE)
}
\arguments{
\item{df}{A data frame containing the survey data}

\item{ques}{Character string specifying the column name of the question to analyze}

\item{disag}{Character string specifying the disaggregation variable name}

\item{level}{Character string specifying the disaggregation level}
}
\value{
A data frame with mean value and metadata
}
\description{
Collection of functions for statistical analysis of numeric survey data.
These functions calculate various descriptive statistics for integer-type questions.
Each function can be used standalone or as part of the main analysis workflow.
}
\examples{
# Create sample data
survey_data <- data.frame(
  age = c(25, 30, 35, 40, 28, 32, 45, 38),
  region = c("North", "South", "North", "South", "East", "North", "South", "East")
)

# Calculate mean age overall
mean_age <- stat_mean(df = survey_data, 
                     ques = "age", 
                     disag = "all", 
                     level = "all")
print(mean_age)

# Calculate mean age by region
mean_age_north <- stat_mean(df = survey_data, 
                           ques = "age", 
                           disag = "region", 
                           level = "North")
print(mean_age_north)
# Calculate median age
median_age <- stat_median(df = survey_data, 
                         ques = "age", 
                         disag = "all", 
                         level = "all")
print(median_age)
}
